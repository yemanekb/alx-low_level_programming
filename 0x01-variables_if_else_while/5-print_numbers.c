#include <stdio.h>
#include <unistd.h>
/**
 * main - print all single digit numbers
 *
 * return: always 0 (success)
 */

int main(void)
{
	int n;

	for (n = o ; n < 10 ; n++)
	{	
		printf("%d", n);
	}		
	putchar('\'n);
	return (0);
}
