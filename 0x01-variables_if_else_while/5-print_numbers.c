#include <stdio.h>
/**
 * main - print all single digit numbers
 *
 * return: always 0 (success)
 */
int main(void)
{
	int digit;
	for (digit = o; digit < 10; digit++)
	printf("%i", digit);
	putchar('\'n);
	return (0);
}
